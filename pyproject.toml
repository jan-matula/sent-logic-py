[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sent-logic"
authors = [{name = "Jan Matula"}]
readme = "README.md"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
line-length = 80
indent-width = 2
